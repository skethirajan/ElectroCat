# Defines environment variables for configuring development tools.
# This file is loaded automatically by the Makefile (`include .dev.env`) and can also
# be sourced in a shell using:
#     set -a; source .dev.env; set +a
#
# Use this to set paths, versioning, and tool-specific settings (e.g., uv, Conda).
# Extend with additional environment-level variables as needed.

# Conda environment name and Python version
CONDA_ENV=ecat
PYTHON_VERSION=3.12.9

# Nested virtual environment location (created via `uv venv`)
VENV_DIR=.venv

# Configure uv cache location
UV_CACHE_DIR=.uv_cache
UV_LINK_MODE=copy

# Configure pre-commit hooks
PRE_COMMIT_HOME=.pre-commit_cache
